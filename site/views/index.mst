{{> header}}
  <div class="container py-5">
    <div class="row">
        <div class="col s3">
          {{> sidebar}}
        </div>
        <div class="col s9">
          <h1>{{title}}</h1>
          {{> search}}
          {{> form}}
          {{> table}}      
        </div>
    </div>
    </div>
{{> footer}}
